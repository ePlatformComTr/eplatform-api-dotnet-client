namespace ePlatform.Api.eBelge.Ticket.Common.Enums
{
    public enum TicketStatus
    {
        Draft = 0,
        Deleted = 10,
        Queued = 20,
        Error = 40,
        Approved = 60,
        Canceled = 100
    }
}
